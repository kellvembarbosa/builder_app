import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:builder_app/app/providers/wordpress/wordpress_page.dart';

void main() {
  testWidgets('WordpressPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(WordpressPage(title: 'Wordpress')));
    //  final titleFinder = find.text('Wordpress');
    //  expect(titleFinder, findsOneWidget);
  });
}
