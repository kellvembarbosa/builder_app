import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:builder_app/app/providers/wordpress/wordpress_controller.dart';
import 'package:builder_app/app/app_module.dart';

void main() {
  initModule(AppModule());
  // WordpressController wordpress;
  //
  setUp(() {
    //     wordpress = AppModule.to.get<WordpressController>();
  });

  group('WordpressController Test', () {
    //   test("First Test", () {
    //     expect(wordpress, isInstanceOf<WordpressController>());
    //   });

    //   test("Set Value", () {
    //     expect(wordpress.value, equals(0));
    //     wordpress.increment();
    //     expect(wordpress.value, equals(1));
    //   });
  });
}
